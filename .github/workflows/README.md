# Workflow placeholders

CI workflows will be added after the server/frontend layout is finalized. Keep future workflows scoped to their target package and avoid production calls or external email during tests.
