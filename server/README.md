# Server boundary

`server/` is the future deployment boundary. The current deployable surface is `server/frontend`; `server/backend` is an integration placeholder until the backend line is reviewed and transferred separately. Worker stages and shared contracts stay independent of `research/`.
