# Backend integration placeholder

This directory reserves the deployable backend location for the separate HUJ integration. No backend implementation, credentials, package manifest, or runtime data is included in this structure-only branch.

When the backend is transferred, keep its real and mock modes explicit, keep research code out of the runtime dependency graph, and ensure tests do not send email or call production services.
