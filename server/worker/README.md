# Worker boundary

Worker stages are separated by collection, detection, and evaluation. Implementations should communicate through `server/contracts/` and remain independently deployable from research code.
